#!/bin/sh

if [ $# -eq 0 ]; then
	if [ -n "$DISPLAY" ]; then
		exec emacsclient -a '' -c -n
	else
		exec emacsclient -a ''
	fi
fi

if emacsclient -n "$@" 2>/dev/null; then
	: ok
elif [ -n "$DISPLAY" ]; then
	emacsclient -a '' -n "$@"
else
	emacsclient -a '' "$@"
fi
